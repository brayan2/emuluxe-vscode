{
  "name": "emuluxe-vscode",
  "displayName": "Emuluxe: Mobile Simulation",
  "description": "Powerful mobile device simulation directly in VS Code and Cursor.",
  "version": "1.0.0",
  "publisher": "emuluxe",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "emuluxe.start",
        "title": "Emuluxe: Start Simulation",
        "category": "Emuluxe"
      },
      {
        "command": "emuluxe.login",
        "title": "Emuluxe: Login",
        "category": "Emuluxe"
      },
      {
        "command": "emuluxe.stop",
        "title": "Emuluxe: Stop Simulation",
        "category": "Emuluxe"
      }
    ],
    "configuration": {
      "title": "Emuluxe",
      "properties": {
        "emuluxe.apiUrl": {
          "type": "string",
          "default": "https://app.emuluxe.com",
          "description": "Emuluxe API URL"
        },
        "emuluxe.token": {
          "type": "string",
          "default": "",
          "description": "CLI token for authentication"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/vscode": "^1.110.0",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.105.4",
    "webpack-cli": "^7.0.2"
  },
  "dependencies": {
    "axios": "^1.13.6"
  }
}
